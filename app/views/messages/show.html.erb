<p id="notice"><%= notice %></p>

<h1>Chats for <em><%= @user.name %></em> </h1>

<table>
  <thead>
  <tr>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @chat.each do |chat| %>
    <tr>
      <td><%= User.find(chat["sender_uid"]).name %>:   </td>
      <td><%= chat["message"] %>   </td>
    </tr>
  <% end %>

  </tbody>
</table>

<br>

<!%= link_to 'Edit', edit_message_path(@message) %>
<%= link_to 'Back to Message List', messages_path %>
<%= link_to 'Back to Match List', matchmake_index_path %>

