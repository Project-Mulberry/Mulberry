<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Blog</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

  </head>
  <body>
    <%# These classes come from the Bootstrap CSS framework. %>
    <nav class="navbar navbar-expand-lg  text-white mb-3 " style="background-color: #536DFE;">
      <ul>
        <% if logged_in? %>
          <ul class="nav navbar-right " style="background-color: #536DFE;">
            <p style="color: white; text-align: right ">Logged In As: <%= current_user&.name || current_user.phone %></p>
            <li><%= link_to "Matches", matchmake_index_path, class: "mx-2" %></li>
            <li><%= link_to "Chats", messages_path, class: "mx-2" %></li>
            <li><%= link_to "Profile", user_path(@current_user), class: "mx-2" %></li>
            <li><%= link_to "Log out", logout_path, class: "mx-2", method: :delete %></li>
          </ul>
        <% else %>
          <ul class="nav navbar-right ">
            <li><%= link_to "Sign up", signup_path, class: "btn btn-primary" %></li>
            <li><%= link_to "Log In", login_path, class: "btn btn-primary" %></li>
          </ul>
        <% end %>
      </ul>
    </nav>
    <div class="container">
      <main id="main">
        <%= yield %>
        <% flash.each do |type, msg| %>
          <div>
            <%= msg %>
          </div>
        <% end %>
      </main>
    </div>
  </body>

  <style>
      ul {
          list-style-type: none;
          margin: 0;
          padding: 0;
          overflow: hidden;
          background-color: #333;
      }
      li {
          float: left;
      }
      li a {
          display: block;
          color: white;
          text-align: center;
          padding: 14px 16px;
          text-decoration: none;
      }
      li a:hover {
          background-color: #111;
      }
  </style>

</html>

