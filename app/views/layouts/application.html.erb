<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Blog</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

  </head>
  <body>
    <%# These classes come from the Bootstrap CSS framework. %>
    <nav class="navbar navbar-expand-lg  text-white mb-3 " style="background-color:#6f42c1;">
      <a class="navbar-brand" style="color: white" href="/"> MULBERRY </a>
      <div class="collapse navbar-collapse" id="navbarText">
        <% if logged_in? %>
          <ul class="navbar-nav mr-auto " style="background-color: #6f42c1;">
            <li class="nav-item"> <%= link_to "Matches", matchmake_index_path %> </li>
            <li class="nav-item"> <%= link_to "Chats", messages_path %> </li>
            <li class="nav-item"> <%= link_to "Profile", user_path(@current_user) %> </li>
            <li class="nav-item"> <%= link_to "Log out", logout_path, method: :delete %> </li>
          </ul>
          <span class="navbar-text" style="color: white">
            Logged In As: <%= current_user&.name || current_user.phone %>
          </span>
        <% else %>
          <ul class="nav navbar-right ">
            <li><%= link_to "Sign up", signup_path, class: "btn btn-primary" %></li>
            <li><%= link_to "Log In", login_path, class: "btn btn-primary" %></li>
          </ul>
        <% end %>
      </div>
    </nav>
    <div class="container">
      <main id="main">
        <%= yield %>
        <% flash.each do |type, msg| %>
          <div>
            <%= msg %>
          </div>
        <% end %>
      </main>
    </div>
  </body>

  <style>
      ul {
          list-style-type: none;
          margin: 0;
          padding: 0;
          overflow: hidden;
          background-color: #333;
      }
      li {
          float: left;
      }
      li a {
          display: block;
          color: white;
          text-align: center;
          padding: 14px 16px;
          text-decoration: none;
      }
      li a:hover {
          background-color: #111;
      }
  </style>

</html>

